# 给 Claude 的交待

这个仓库是一张历史地图:中国电子工业的厂、所、院校,按年份落在图上。
数据从地方志来 —— 扫描件 → 转换稿 → 人工核对 → `CN_Electronic_Industry.xlsx`。

## 一、**动手一律走插件**

除了**装更新**那一件事(`tools/obsidian-plugin/装.ps1` 要在 PowerShell 里跑),
其余每一步都在 Obsidian 插件的面板上点 —— `tools/obsidian-plugin/`。

所以:

* **别给 PowerShell 命令行当办法。** 要人敲 `python tools/gazetteer/gaz.py …`
  的答复是没答完:那一步得在面板上有个按钮。
* **`gaz` 添了子命令、改了参数,面板要跟着添一条。** 面板上没有的功能,
  等于没有 —— 文档里写得再清楚也到不了人手里。
* **面板上看得见的字,不许是机器话。** `git status` 默认把中文文件名escape成
  八进制(`\350\275\254…`),那种东西摆到面板上就是让人对着一串数字猜
  自己的稿子叫什么。见 `src/runner.ts` 里那几个 `GIT_CONFIG_*`。
* 判断的活儿仍归人:「取否」写 `y` 的行才收。插件替人少敲字,不替人点头。

`tools/gazetteer/电子工业地图流程.md` 是**正本**,插件是它的壳子。
改了参数、改了步骤,回去改那份文档。

## 二、誊录的规矩

* **地址只抄志书原文。** 志书没写就空着 —— 空着是实情,从别处填上就成了另一回事。
* **产品宁滥勿缺,单位宁缺勿滥。** 产品多收几条,核对时删得掉;
  单位收错一家,图上就多一个不存在的点。
* **核过的不再核。** 已经核过、已在总表里的行,重跑时标「已收」,
  新认出来的只摆那几个格子(见《重跑一本核过的志书》)。
* **认不出就空着,不猜。** 隶属两说、表题并了两类、只说「直属」没说直属于谁 ——
  一概空着,备注里说明缘由,等人来判。

## 三、几处踩过的坑

* Windows 的 PowerShell 5.1 把不带 BOM 的 UTF-8 当 ANSI 读 —— `.ps1` 存 BOM。
* GBK 里没有「✓」,接了管道就炸。见 `gazetteer/console.py`。
* 参数一律按数组传给子进程,不拼成一行字。
  `《北京工业志·电子志》2001 第四篇.md` 书名号、中文、空格三样全占。
* 待核工作簿是二进制,git 冲突了只能留一边 —— 拉取之前先提交。
