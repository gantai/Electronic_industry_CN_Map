node_modules/
dist/
.DS_Store
*.log
gaz-work/
*.backup-*.xlsx
__pycache__/
*.pyc

# geoBoundaries 原样下载的那一包几十上百 MB(还带着 shapefile 与 zip),
# git 会把它永远留在历史里 —— 删掉也还在。先用
# tools/geo/添底图.py --save-subset 切成一市,再提交那一份小的。
# 不限目录:下载落在哪儿都挡得住。
geoBoundaries-*/
geoBoundaries-*.zip
**/geoBoundaries-CHN-ADM*
底图源/*_simplified.geojson
底图源/*_ADM*.geojson

# 更新前另存的那一份(见《更新工具》)—— 对完就删,不入库
我的改动*.xlsx

# 志书原件。**原件搁在库里(D:\Archive\材料),不搁仓库。**
# 一本扫描的志书几百 MB,git 会把它永远留在历史里 —— 删掉也还在,
# 跟 geoBoundaries 那一包是同一个道理。进仓库的是转出来的稿子
# (转换稿\*.md,几十 KB),那一份才是抽取的凭据,也才查得回去。
# 面板上「提交改动」跑的是 git add -A,这一行就是那道保险。
*.pdf
