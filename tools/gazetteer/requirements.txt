# 装法见 README。四样都不是必需的,缺什么 `gaz check` 会说。
openpyxl>=3.1          # 回写 CN_Electronic_Industry.xlsx（gaz xlsx 必需）
pymupdf>=1.24          # PDF 翻页、渲染、取文本层;也可改用系统的 poppler
paddleocr>=2.7         # 中文 OCR 首选
paddlepaddle>=2.6      # PaddleOCR 的运行时
